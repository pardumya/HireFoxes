<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hire.foxes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HireFoxes">
        <activity android:name=".bottom_navigation.profile_fragments.profile.edit_profile"></activity>
        <activity android:name=".bottom_navigation.profile_fragments.settings.add_your_place" />
        <activity android:name=".bottom_navigation.profile_fragments.payment.payment" />
        <activity android:name=".bottom_navigation.profile_fragments.bookmarks.bookmarks" />
        <activity android:name=".bottom_navigation.profile_fragments.settings.settings" />
        <activity android:name=".bottom_navigation.profile_fragments.notification.notification" />
        <activity android:name=".bottom_navigation.profile_fragments.about" />
        <activity android:name=".bottom_navigation.profile_fragments.profile.profile" />
        <activity android:name=".bottom_navigation.home_fragments.ViewAll" />
        <activity android:name=".signUp" />
        <activity android:name=".login" />
        <activity android:name=".bottom_navigation.provider_profile" />
        <activity android:name=".bottom_navigation.provider" />
        <activity android:name=".bottom_navigation.home_fragments.searchView" />
        <activity android:name=".Loading">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".index" />
    </application>

</manifest>